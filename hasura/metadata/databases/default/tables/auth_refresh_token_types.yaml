table:
  name: refresh_token_types
  schema: auth
array_relationships:
  - name: refresh_tokens
    using:
      foreign_key_constraint_on:
        column: type
        table:
          name: refresh_tokens
          schema: auth
